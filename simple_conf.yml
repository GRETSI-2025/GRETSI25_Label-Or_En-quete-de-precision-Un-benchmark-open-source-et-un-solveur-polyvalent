objective:
  - "GLasso[reg=0.1]"
solver:
  - sklearn
  - skglm[algo=banerjee]
dataset:
  # - simulated[n_samples=500,n_features=200]
  - simulated[n_samples=200,n_features=100]
  # - simulated[n_samples=200,n_features=50]
n-repetitions: 3
