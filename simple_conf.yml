objective:
  - "GLasso[reg=0.1]"
solver:
  - skglm[algo=banerjee,lasso_solver=["anderson_cd_numba","cd_numba"],inner_tol=[1e-4]]
  - sklearn[inner_tol=[1e-4]]
dataset:
  - simulated[n_samples=1000,n_features=100]
n-repetitions: 100 # 3 doesn't seem enough to account for variability
n-jobs: 1
